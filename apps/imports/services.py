"""Legacy import service module intentionally contains no generic importer.

The governed student onboarding implementation lives in ``apps.imports.onboarding``.
This module remains only as an empty package boundary for operational tooling;
no compatibility functions or duplicate batch path are provided.
"""
