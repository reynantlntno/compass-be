from django.apps import AppConfig


class SupportNeedsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.support_needs"
    label = "support_needs"
    verbose_name = "Student Support Needs"
